import { UserNameValidatiors } from './user-name-validatiors';

describe('UserNameValidatiors', () => {
  it('should create an instance', () => {
    expect(new UserNameValidatiors()).toBeTruthy();
  });
});
